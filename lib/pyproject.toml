[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "insigne"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "sqlalchemy>=2.0",
    "bcrypt>=4.0",
    "PyJWT>=2.8.0",
    "pyyaml>=6.0",
    "jinja2>=3.1.0",
]
